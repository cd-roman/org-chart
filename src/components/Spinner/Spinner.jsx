import React from "react";
import "primeicons/primeicons.css";
import { SpinnerContainer } from "./Spinner.styles.jsx";

const Spinner = () => (
  <SpinnerContainer>
    <i className="pi pi-spin pi-spinner"></i>
  </SpinnerContainer>
);

export default Spinner;
